<Window x:Class="TaskMangementSystem.Views.AddCommentView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TaskMangementSystem.Views"
        mc:Ignorable="d"
        Title="AddCommentView" Height="450" Width="800">
    <Grid>

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Task details -->
        <TextBlock Grid.Row="0" Text="Add Comment" Margin="5"/>

        <!-- Text box for entering comment -->
        <TextBox Grid.Row="1" Text="{Binding NewCommentText, UpdateSourceTrigger=PropertyChanged}" Margin="5"/>

        <!-- Button to add comment -->
        <Button Grid.Row="2" Content="Add Comment" Command="{Binding AddCommentCommand}" Margin="5"/>

    </Grid>
</Window>
